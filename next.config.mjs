/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["lh3.googleusercontent.com", "localhost", "daisyui.com"],
      },
      reactStrictMode: true,
};

export default nextConfig;
